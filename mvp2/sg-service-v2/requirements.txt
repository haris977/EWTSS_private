fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0
pytest==8.2.2
pytest-asyncio==0.23.7
anyio==4.4.0
Pillow>=10.0.0
numpy>=1.26.0
scipy>=1.13.0

# agi.stk13 is the AGI/Ansys COM binding for STK 13 (direct successor of
# agi.stk12). NOT installed from PyPI — the wheel ships inside the STK
# install. Install it into the same venv after the dependencies above:
#
#   pip install "C:\Program Files\AGI\STK_ODTK 13\bin\AgPythonAPI\agi_stk13-13.1.0-py3-none-any.whl"
#
# Version suffix (e.g. 13.1.0) may vary with the installed STK build.
