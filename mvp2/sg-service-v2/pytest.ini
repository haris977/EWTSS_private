[pytest]
# Silence a PendingDeprecationWarning from starlette's form-parser import
# — we don't use multipart form parsing, so the upstream dep path is noise.
filterwarnings =
    ignore::PendingDeprecationWarning:starlette.formparsers
