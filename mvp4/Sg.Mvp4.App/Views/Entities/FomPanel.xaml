<UserControl x:Class="Sg.Mvp4.App.Views.Entities.FomPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:Sg.Mvp4.Domain.Models;assembly=Sg.Mvp4.Domain">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Margin="8" FontWeight="Bold"
                   Text="{Binding EntityName, StringFormat='Figure of Merit: {0}'}" />

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="8">
            <Button Content="Reset" Command="{Binding ResetCommand}" Width="70" Margin="4,0" />
            <Button Content="Apply" Command="{Binding ApplyCommand}" Width="70" />
        </StackPanel>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="8">

                <TextBlock Text="Type" FontWeight="SemiBold" Margin="0,0,0,4" />
                <ComboBox ItemsSource="{Binding Source={x:Static models:FomKindValues.All}}"
                          SelectedItem="{Binding Kind, Mode=TwoWay}" />

                <TextBlock Text="Statistic" FontWeight="SemiBold" Margin="0,12,0,4" />
                <ComboBox ItemsSource="{Binding Source={x:Static models:FomStatisticValues.All}}"
                          SelectedItem="{Binding Statistic, Mode=TwoWay}" />

            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
